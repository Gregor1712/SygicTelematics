apiVersion: apps/v1
kind: Deployment
metadata:
  name: trip-api
  namespace: sygic-telematics
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trip-api
  template:
    metadata:
      labels:
        app: trip-api
    spec:
      containers:
        - name: trip-api
          image: sygic-registry:5000/trip-api:latest
          ports:
            - containerPort: 5106
              name: http
            - containerPort: 6106
              name: grpc
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Docker"
            - name: HttpPort
              value: "5106"
            - name: GrpcPort
              value: "6106"
            - name: ConnectionStrings__DefaultConnection
              valueFrom:
                secretKeyRef:
                  name: sygic-db-secret
                  key: ConnectionString__Trip
          envFrom:
            - configMapRef:
                name: sygic-config
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          readinessProbe:
            httpGet:
              path: /health
              port: 5106
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 5106
            initialDelaySeconds: 30
            periodSeconds: 15
---
apiVersion: v1
kind: Service
metadata:
  name: trip-api
  namespace: sygic-telematics
spec:
  selector:
    app: trip-api
  ports:
    - port: 5106
      targetPort: 5106
      name: http
    - port: 6106
      targetPort: 6106
      name: grpc
  type: ClusterIP
